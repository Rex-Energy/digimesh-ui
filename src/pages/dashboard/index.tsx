// @ts-nocheck

import { Grid } from "@mui/material";
import Layout from "./Layout";

export default function Dashboard() {
  return (
    <Layout>
      <Grid>Dashboard</Grid>
    </Layout>
  );
}
